{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row" style="text-align: center;">
            <h1>Bienvenue chez Schoolium</h1>
            {% if  app.user %}
            <h1>{{app.user.nom}}</h1>
            {% endif %}
        </div>
    </div>
{% endblock %}